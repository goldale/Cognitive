kind: T_Content
section: S_03_02
blocks:
- type: paragraph
  text: The internal language is obtained from a selected intermediate Transformer layer. The architecture does
    not prescribe one universal layer number.
- type: principle
  title: Implementation-dependent layer selection
  text: Transformer specialists must select and justify the layer, layer combination, or learned projection that
    best preserves semantic stability, compositionality, context, and bidirectional usability by memory.
- type: table
  headers:
  - Required property
  - Reason
  rows:
  - - Semantic stability
    - Equivalent meanings should produce sufficiently compatible memory interactions.
  - - Context sensitivity
    - The same element in different contexts must remain distinguishable.
  - - Bidirectional usability
    - Memory output must be consumable by later Transformer processing.
  - - Dimensional stability
    - Persistent memory must not depend on incidental runtime tensor layout.
  - - Task relevance
    - The representation must retain distinctions needed by the trained Transformer and its objectives.
- type: warning
  title: Not an arbitrary hidden state
  text: A raw hidden state is not automatically a suitable architectural interface. The selected representation
    requires empirical validation and may need a stable normalization or projection.
- type: paragraph
  text: The interface is Transformer-specific. Replacing Transformer may require migration, adaptation, or reconstruction
    of memory because another model may define a different internal semantic space.
- type: heading
  level: 3
  text: Asymmetric Extraction and Reinjection
- type: principle
  title: Asymmetric Transformer interface
  text: The point that extracts a memory-facing internal-language state and the point that receives serialized memory
    are distinct. External input must first be translated by early Transformer layers. Memory output is already
    expressed in the internal language and is therefore reinjected deeper, into layers responsible for semantic
    integration, reasoning, planning, and response formation.
- type: formula
  text: external input -> early interpretation -> internal-language extraction point -> READ
- type: formula
  text: serialized memory -> deeper reinjection point -> integration and reasoning
- type: principle
  title: No repeated translation
  text: Serialized memory must not be routed through the same early layers that translate raw external form. Reprocessing
    internal-language memory as external input is redundant and may distort the preserved semantic state.
- type: note
  title: Working depth hypothesis
  text: A working hypothesis is that the Memory Reinjection Point should be approximately 20–30% of Transformer
    depth later than the Internal-Language Extraction Point. The exact offset is implementation-dependent and must
    be established experimentally for each Transformer family.
- type: diagram
  title: Asymmetric Transformer–Memory Interface
  description: Early Transformer layers interpret the external request. Transformer formulates and sends READ. Memory
    retrieves and serializes information using the current D-Context from STM. READ content returns as a
    serialized message in the Internal Language and is reinjected into deeper Transformer layers. Transformer then
    formulates and sends UPDATE in the Internal Language.
  direction: LR
  size: extra-large
  readability_priority: true
  proportionality_priority: true
  nodes:
  - id: external
    label: External Request
    kind: external
  - id: early
    label: 'Early Transformer Layers

      interpret external request'
    kind: process
  - id: read
    label: 'Transformer formulates

      and sends READ request'
    kind: control
  - id: stm
    label: 'STM

      current D-Context'
    kind: state
  - id: memory
    label: 'Memory

      LTM1 + LTM2'
    kind: storage
  - id: serialize
    label: 'Information Retrieval

      and Serialization'
    kind: process
  - id: readmsg
    label: 'READ Content

      serialized Internal Language message'
    kind: interface
  - id: deep
    label: 'Deep Transformer Layers

      integration and reasoning'
    kind: subsystem
  - id: update
    label: 'Transformer formulates

      and sends UPDATE message

      in the Internal Language'
    kind: control
  edges:
  - from: external
    to: early
    label: external request
    flow: information
  - from: early
    to: read
    label: intermediate representation
    flow: information
  - from: read
    to: memory
    label: READ request
    flow: control
  - from: stm
    to: serialize
    label: D-Context defines subset
    flow: context
  - from: memory
    to: serialize
    label: candidate information
    flow: information
  - from: serialize
    to: readmsg
    label: serialize selected subset
    flow: information
  - from: readmsg
    to: deep
    label: deeper reinjection
    flow: context
  - from: early
    to: deep
    label: current request state
    flow: information
  - from: deep
    to: update
    label: resulting internal state
    flow: information
  - from: update
    to: stm
    label: update D-Context
    flow: learning
  - from: update
    to: memory
    label: change Memory state
    flow: learning
- type: table
  headers:
  - Interface point
  - Function
  rows:
  - - Internal-Language Extraction Point
    - Produces the shared internal representation used to initiate READ and to form UPDATE content.
  - - Memory Reinjection Point
    - Receives serialized memory after early external-form interpretation and integrates it into later Transformer
      computation.
