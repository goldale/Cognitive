# Cognitive 0.3.11 Release Notes

## Navigation refinement

- Rebuilt all HTML documentation with a single-line top and bottom navigation bar.
- Renamed `A–Z Alphabetical Index` to `A–Z Index`.
- Corrected the navigation grid from four to five columns.
- Prevented navigation wrapping on narrow viewports; compact horizontal scrolling is used only when required.
- Removed the separate footer-description row so the footer navigation occupies one line.
- Added regression coverage for the short label and single-line layout.
